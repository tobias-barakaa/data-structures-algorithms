#include <stdio.h>

int main() {
    int i;  // Use an integer variable for the loop

    for (i = 65; i <= 90; i++) {
        putchar(i);  // Print digits by adding '0' to the loop variable
    }
    int num = 3;
    if (num == 3)
    {
     
    }

    return 0;
}